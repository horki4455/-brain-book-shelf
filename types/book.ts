export interface Books {
  id: string
  title: string
  author: string
  finishDay: string
  price: string
  published: string
  status: string
  think: string
  userId: string
  ratingVal: number
}

export interface SelectedBook {
  id: string
  title: string
  author: string
  price: string
  published: string
}
